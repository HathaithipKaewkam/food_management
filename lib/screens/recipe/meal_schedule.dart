import 'package:flutter/material.dart';

class MealScheduleScreen extends StatelessWidget {
  const MealScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fertilizer Screen'),
    );
  }
}